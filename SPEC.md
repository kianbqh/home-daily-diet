# 今天想吃啥：家庭菜品记录与选餐 SPEC

**版本：** 0.1.5  
**状态：** 当前实现基线  
**平台：** 微信小程序 + 腾讯云 CloudBase

## 1. 产品目标

家庭成员把家里做过的菜记录进菜品库，之后从已有菜品中提交“今天想吃什么”，再确认当天菜单。产品重点是保存家庭味道、减少重复沟通，并让每次吃饭都能逐步完善菜品库。

核心顺序固定为：

```text
先记录菜品 → 菜品进入菜品库 → 家庭成员提交想吃的菜 → 确认当天菜单
```

## 2. 当前版本范围

### 家庭空间

- 一个微信用户可以初始化一个家庭空间。
- 家庭成员通过 6 位短邀请码加入。
- 邀请码由 CloudBase 云函数生成，有效期 30 天，可撤销和重新生成。
- 家庭 ID 只作为内部标识，不在用户界面、剪贴板或分享路径中出现。
- 家庭成员不区分角色，每个人都可以记录菜品、追加制作记录和提交选餐。
- 家庭名称和成员称呼可以在家庭页修改。

### 菜品库

- 菜名是记录菜品的唯一必填字段。
- 每次记录最多上传一张照片，照片可选。
- 没有照片的菜品使用统一的无图卡片呈现，不影响搜索和选餐。
- 同一道菜可以追加多次制作记录，历史记录不被覆盖。
- 记录页不出现评价、吃起来怎么样或备注输入板块。
- 记录页可以查看历史制作日期、餐次和照片。
- 图片上传失败时仍然保存菜名和其他文字信息。

### 当天选餐

- 只能从菜品库中选择，不能在选餐页临时创建菜品。
- 每位家庭成员可以提交一道或多道想吃的菜。
- 所有家庭成员可以查看当前候选。
- 菜单确认后锁定当天最终菜品，不再修改该次选择。
- 选餐分享链接使用短邀请码，不携带裸家庭 ID。

### 首页与引导

- 首页优先显示当天选餐状态和菜品库入口。
- 菜品库为空时显示“记录第一道菜”的空状态。
- 第一道菜保存后提供“去选今天吃什么”“继续记录下一道”“看看菜品库”三个入口。
- 不使用强制多步教程，不加入内置聊天。

## 3. CloudBase 架构

### 集合

```text
family_states          家庭共享状态快照
family_states_events   家庭状态事件快照
family_members         openid 与家庭成员关系
family_invites         短邀请码、有效期和撤销状态
```

### 云函数

云函数名称：`family-access`。

支持动作：

- `bootstrap`：首次绑定当前微信用户与家庭成员。
- `createInvite`：生成或返回当前有效邀请码。
- `getInvite`：获取当前家庭邀请码。
- `revokeInvite`：撤销当前邀请码。
- `acceptInvite`：使用短邀请码加入家庭。
- `load`：验证成员身份后读取家庭状态。
- `save`：验证成员身份后合并保存家庭状态。

云函数从调用上下文读取 `OPENID`，客户端不能指定或覆盖调用者身份。非成员不能通过家庭 ID 读取或保存家庭状态。

### 同步策略

- 本地状态先保存，再排队同步到云端。
- 云端 hydration 使用按实体 ID 的合并，不直接覆盖本地状态。
- hydration 期间发生的新编辑保留并再次排队保存。
- 网络或云函数失败时继续使用本地数据，并展示同步错误状态。
- 保存云端时移除本地专用字段 `currentMemberId`。

## 4. 当前配置

```text
AppID: wx6e247df29f902c68
环境 ID: home-daily-diet-d8f5e7d6907dd53a
区域: ap-shanghai
状态集合: family_states
事件集合: family_states_events
成员集合: family_members
邀请码集合: family_invites
云函数: family-access
```

## 5. 明确不在当前版本

- 内置聊天、群机器人和公开社区。
- 语音识别、原始语音保存和 LLM 菜谱解析。
- 家庭管理员、角色权限、成员移除和家庭转移。
- 公开家庭内容、陌生人搜索和跨家庭推荐。
- 营养分析、采购清单、支付和外卖能力。
- 生产级备案、运营后台和面向公众的发布体系。

## 6. 后续版本方向

第二阶段可以增加语音录入：保留原始语音和转写文本，由 LLM 解析食材、用量、步骤、火候、技巧和常见失败原因，生成待确认的菜谱草稿。未经家庭成员确认的解析结果不能直接覆盖已保存内容。

## 7. 验收标准

- 家庭页不显示长家庭 ID。
- 生成、复制、分享、撤销和重新生成邀请码可用。
- 第二台设备使用有效邀请码可以加入并看到家庭数据。
- 无效、过期和撤销邀请码会得到明确提示。
- 菜名可以在没有图片的情况下保存。
- 记录页没有评价和备注输入或展示板块。
- 云端同步失败不清空本地数据。
- `npm test`、`node scripts/smoke-check.js` 和 JavaScript 语法检查通过。
