# 家庭饮食记录与选餐小程序 SPEC

**版本：** 1.0  
**状态：** 已完成需求共识确认  
**日期：** 2026-08-02  
**目标平台：** 微信小程序  
**后端方向：** 微信小程序原生前端 + 腾讯云 CloudBase

## 1. 产品概述

这是一个只供家庭成员使用的饮食记录与选餐小程序。

它要解决的核心问题是：家人经常互相询问“今天吃什么”，但过去做过的好菜没有被持续记录，久而久之又回到重复的几道菜。小程序通过建立家庭自己的菜品库，让家人先记录菜品，再从已有菜品中选择当天想吃的内容，并把结果分享到家庭群。

第一版的目标不是做公开菜谱社区，也不是做聊天工具，而是建立一个真实可持续使用的家庭菜品记忆库，并减少“今天吃什么”的沟通成本。

## 2. 用户与使用范围

### 2.1 目标用户

- 仅限一个家庭内部的成员。
- 家长和其他家人都可以新增菜品、记录制作、选择想吃的菜、评价和补充备注。
- 不区分管理员、家长、普通成员等复杂角色。
- “当天负责做饭的人”可以确认当天菜单，但这只是操作场景，不是系统权限角色。

### 2.2 家庭空间

- 一名用户创建家庭空间。
- 通过小程序邀请卡或二维码邀请其他家庭成员加入。
- 使用微信登录，不要求手机号，不开放公开搜索。
- 菜品、照片、评价、制作记录和语音内容只对该家庭空间成员可见。

## 3. 核心产品原则

1. **先记录，后选择。** 菜品必须先进入菜品库，之后才能被选为想吃的菜。
2. **记录尽量简单。** 菜名是唯一必填项，图片和其他信息都可以补充。
3. **不覆盖历史。** 同一道菜再次制作时追加新的制作记录，而不是覆盖旧照片或旧评价。
4. **真实家庭内容优先。** 不预置虚构菜品，不做公开内容和复杂推荐。
5. **首页服务当天决策。** 用户打开小程序后，优先看到“今天吃什么”。
6. **不把小程序变成聊天工具。** 复杂讨论继续在家庭群完成，小程序负责记录、选择和沉淀。

## 4. MVP 功能范围

### 4.1 家庭空间

- 创建家庭空间。
- 生成邀请卡或二维码。
- 微信登录并加入家庭空间。
- 查看当前家庭成员。
- 家庭成员可以退出家庭空间。

### 4.2 菜品库

菜品库是整个产品的基础，只有已经记录的菜品才能参与每日选择。

支持：

- 新增菜品。
- 编辑菜名。
- 为菜品追加制作记录。
- 搜索菜名。
- 按简单标签筛选，例如荤菜、素菜、汤、主食。
- 查看最近做过的菜。
- 查看家人最喜欢的菜。
- 查看菜品下的历史制作记录。

菜品卡统一使用固定比例布局：

- 有图片时展示菜品图片。
- 没有图片时展示柔和分类色背景、简单餐盘图标或菜名首字。
- 下方展示菜名、最近制作时间和汇总评价。
- 无图片不能阻止菜品参与每日选择。

### 4.3 记录一道菜

所有新增记录使用同一套流程，不区分“餐后记录”和“回忆补录”。

#### 新菜品

- 菜名：必填。
- 图片：可上传，也可以跳过。
- 记录时间：自动填写，可调整。
- 记录人：自动填写。
- 餐次：可选，支持早餐、午餐、晚餐和自定义餐次。
- 评价：可选。
- 备注：可选。

保存后创建一张菜品卡，并同时创建第一条制作记录。

#### 已有菜品

- 先搜索并选择已有菜品。
- 追加一条新的制作记录。
- 本次记录可以上传一张图片，也可以不上传。
- 本次记录的评价和备注独立保存，不覆盖历史记录。

如果用户输入的菜名与已有菜品相近，应优先提示选择已有菜品，减少重复创建；系统不自动合并含义不确定的菜品。

### 4.4 制作记录与评价

每次制作记录包含：

- 所属菜品。
- 制作时间。
- 记录人。
- 可选的一张图片。
- 可选餐次。
- 可选评价：喜欢、一般、不喜欢。
- 可选备注。

评价绑定在具体制作记录上，菜品卡展示汇总结果。例如，同一道菜第一次“一般”、第二次“喜欢”，两次历史都必须保留。

### 4.5 今日选餐

第一版默认服务晚餐，但菜品记录本身支持其他餐次。

流程：

1. 任意家庭成员发起当天晚餐选择。
2. 每位成员从菜品库中选择一道或多道想吃的菜。
3. 所有成员可以查看当前候选，并在菜单确认前新增、取消或更换自己的选择。
4. 可以把“今天吃什么”页面分享至家庭群。
5. 当天负责做饭的人根据候选、食材、时间和个人意愿确认最终菜单。
6. 确认后当天选择结果锁定，只允许查看。

重要约束：

- 今日选餐页面不允许临时创建新菜品。
- 如果菜品库中不存在想吃的菜，必须先回到菜品库记录该菜，再返回选择。
- 不设置强制截止时间。
- 不做自动提醒；第一版依赖家庭成员手动分享。
- 菜单确认只关联已有菜品，不自动创建新的菜品卡。
- 吃完后可以回到菜品库，为对应菜品追加本次制作记录。

### 4.6 家庭群分享

分享内容为一张小程序卡片，至少包含：

- 日期。
- 餐次，默认晚餐。
- 当前已提交的菜品候选及其图片或占位视觉。
- 进入小程序继续选择的入口。
- 当前菜单是否已经确认。

第一版不做群机器人，不读取群聊内容，也不做内置聊天。

### 4.7 首页

首页按以下优先级组织：

1. 今日晚餐的选择状态。
2. 家人已经提交的菜品候选。
3. “我要吃什么”和“分享给家人”主操作。
4. “记录一道菜”和“浏览菜品库”次要入口。

不优先建设个人主页、消息中心、公开内容或运营模块。

### 4.8 空菜品库引导

首次创建家庭空间且菜品库为空时：

- 不展示虚构的示例菜品。
- 展示“记录第一道家里的菜”的空状态。
- 主按钮为“记录一道菜”。
- 提供“稍后再说”次按钮。
- 第一次保存后提供“继续记录下一道”“查看菜品库”“去选择今天吃什么”“分享给家人”等入口。
- 菜品库为空时，“今天吃什么”页面提示先记录一道菜，而不是展示空白投票页。

不做强制多步教程，也不要求用户一次性录入大量菜品。

## 5. 第二阶段：语音与大语言模型菜谱完善

第二阶段再加入“复刻家里的味道”能力，帮助出国后的家庭成员还原家常做法。

### 5.1 目标

家长只需要对着手机口述做法，系统将口述内容解析为结构化菜谱。

### 5.2 结构化字段

- 食材。
- 用量。
- 分量或适用人数。
- 制作步骤。
- 火候和时间。
- 调味关键点。
- 家庭经验和技巧。
- 常见失败原因或避坑说明。
- 食材替代建议。

### 5.3 处理流程

1. 家长按住或点击按钮开始录音。
2. 语音转文字。
3. LLM 将文本解析为结构化菜谱草稿。
4. 页面展示原始转写和结构化字段。
5. 家长手动修改或继续补充口述内容。
6. 家长确认后保存到对应菜品卡。

LLM 只能生成草稿，不能未经确认直接覆盖或发布菜谱内容。

### 5.4 原始语音

- 保留原始语音。
- 原始语音与对应菜品卡绑定。
- 仅家庭成员可以播放。
- 不对外公开。
- 第二阶段先不做复杂下载和权限控制。

## 6. 数据模型建议

### FamilySpace

- `id`
- `name`
- `createdBy`
- `createdAt`

### FamilyMember

- `id`
- `familyId`
- `wechatOpenId`
- `displayName`
- `joinedAt`

### Dish

- `id`
- `familyId`
- `name`
- `tags`
- `createdBy`
- `createdAt`
- `updatedAt`
- `coverImage`，可为空

### CookingRecord

- `id`
- `familyId`
- `dishId`
- `recordedBy`
- `recordedAt`
- `mealType`，可为空
- `image`，可为空
- `rating`，可为空
- `note`，可为空

### MealSession

- `id`
- `familyId`
- `date`
- `mealType`，第一版默认 `dinner`
- `status`：`open` 或 `confirmed`
- `createdBy`
- `confirmedBy`，可为空
- `confirmedAt`，可为空

### MealSubmission

- `id`
- `mealSessionId`
- `dishId`
- `memberId`
- `status`：`selected` 或 `cancelled`
- `updatedAt`

第二阶段增加：

### RecipeDraft

- `id`
- `familyId`
- `dishId`
- `sourceAudioUrl`
- `transcript`
- `structuredContent`
- `status`：`draft` 或 `confirmed`
- `createdBy`
- `createdAt`
- `confirmedAt`，可为空

## 7. 权限与隐私

- 所有数据必须带有 `familyId`，数据库和云存储按家庭空间隔离。
- 非家庭成员不能读取菜品、照片、制作记录、评价或语音。
- 所有人可以新增、评价和补充备注。
- 原始菜名和图片允许家庭成员修改。
- 删除操作需要二次确认。
- 第一版不提供公开分享链接；小程序分享卡片打开后仍需家庭成员身份才能查看完整内容。

## 8. 技术方案

- 前端：原生微信小程序，优先保持依赖最少。
- 后端：腾讯云 CloudBase。
- 数据库：CloudBase 文档型数据库。
- 图片和语音：CloudBase 云存储。
- 服务逻辑：Cloud Functions。
- 登录：微信登录。
- 分享：微信小程序原生分享能力。
- 第一版不接入 LLM、语音识别、支付、群机器人或独立服务器。

CloudBase 官方产品页当前显示个人版新用户可获得免费体验，之后个人版标价为 19.9 元/月；具体额度和价格在实际开通时重新确认：[CloudBase 产品页](https://cloud.tencent.com/product/tcb)。

## 9. MVP 非目标

第一版明确不做：

- 公开菜谱社区。
- 陌生人注册和搜索。
- 内置聊天。
- 群机器人和群聊内容读取。
- 自动提醒。
- 完整菜谱编辑器。
- 语音识别和大语言模型。
- 复杂权限角色。
- 智能营养分析。
- 购物清单、采购、支付和外卖能力。
- 跨家庭推荐和社交关系。

## 10. 验收标准

### 家庭空间

- 可以创建家庭空间。
- 家人可以通过邀请卡或二维码加入。
- 非家庭成员不能查看家庭内容。

### 菜品记录

- 只填写菜名也可以保存。
- 图片可以上传，也可以跳过。
- 新菜品会创建菜品卡。
- 已有菜品会追加制作记录，不产生重复菜品。
- 菜品卡可以查看历史制作记录。
- 评价和备注可以为空，并且不会覆盖历史记录。

### 今日选餐

- 只能从已有菜品库中选择。
- 菜品库为空时明确引导先记录菜品。
- 家人可以提交多道想吃的菜。
- 确认前可以修改自己的选择。
- 当天负责做饭的人可以确认菜单。
- 确认后菜单不可继续修改。
- 选择页面可以分享到家庭群。

### 菜品展示

- 有图和无图菜品使用同一套卡片布局。
- 无图菜品有美观、直观且简单的占位视觉。
- 可以搜索菜名。
- 可以查看最近做过和家人喜欢的菜。

### 体验目标

- 家里大多数晚餐可以通过小程序完成选择。
- “今天吃什么”的重复群聊明显减少。
- 家人可以在几十秒内找到以前做过的菜。
- 上线后一个月菜品库至少积累 30 道真实家庭菜品。
- 家人愿意持续记录，而不是只使用几天。

## 11. 后续开发顺序建议

1. 创建微信小程序项目和 CloudBase 环境。
2. 完成家庭空间、微信登录和邀请加入。
3. 完成菜品库、菜品卡和制作记录。
4. 完成图片上传、无图占位卡片和评价。
5. 完成今日晚餐选择、确认和家庭群分享。
6. 用真实家庭成员进行一轮试用。
7. 根据实际使用情况优化录入速度和菜品查找。
8. 第二阶段再开发语音、转写和 LLM 菜谱完善。

